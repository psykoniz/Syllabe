node_modules/
dist/
.projectos/
*.db
*.db-journal
# dogfood workspaces — ephemeral run artifacts, not part of the codebase
dogfood-*/
# eval run outputs — keep baseline.json but not per-run results
evals/results/*/
evals/results/*.json
!evals/results/baseline.json
.claude/settings.local.json
.env*
.agent/*
!.agent/lessons.json

# SWE-bench official harness output (per-instance Docker build/run logs).
# Transient evaluation artifacts — the scores live in evals/results/.
logs/
